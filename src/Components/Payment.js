import React from 'react';
import PaymentConfirmation from './PaymentConfirmation'; // Assuming PaymentConfirmation.js is in the same directory

const Payment = () => {
    return (
        <div>
            {/* Display the PaymentConfirmation component here */}
            <PaymentConfirmation />
        </div>
    );
};

export default Payment;
